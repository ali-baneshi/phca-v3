edges:
  - id: E001
    cause: PredictionEngine
    effect: action_diversity
    intervention: enable_prediction=false
    predicted_change: "strategy_diversity increases; transfer_efficiency decreases"
    observed_delta: "strategy_diversity +0.446; transfer_efficiency +0.174 (causal_fair confound); phi_iq +0.087"
  - id: E002
    cause: MDIM
    effect: goal_complexity_L3
    intervention: enable_mdim=false
    predicted_change: "self_generated_goals -> 0; drive diversity collapses"
    observed_delta: "self_generated_goals 0->0; phi_iq -0.150"
  - id: E003
    cause: M3_Consolidation
    effect: cross_context_reuse
    intervention: enable_consolidation=false
    predicted_change: "cross_context_reuse < 0.2 under goal relocation"
    observed_delta: "cross_context_reuse measured via env_goal_relocated trace flag (grid10, dynamic_goals_every=50)"
  - id: E004
    cause: Attention
    effect: adaptation_speed
    intervention: enable_attention=false
    predicted_change: "adaptation_speed decreases; uniform G' updates"
    observed_delta: "not isolated in validation suite"
  - id: E005
    cause: TSPL
    effect: adaptation_speed
    intervention: enable_tspl=false
    predicted_change: "adaptation_speed decreases; skill compilation stalls"
    observed_delta: "not isolated in validation suite"
  - id: E006
    cause: RBTA
    effect: strategy_diversity
    intervention: "resource_policy=energy with tight bounds"
    predicted_change: "TERMINATE rate increases; exploration decreases"
    observed_delta: "strategy_diversity 0; phi_iq -0.015"
  - id: E007
    cause: APC
    effect: goal_stability
    intervention: enable_apc=false
    predicted_change: "goal thrashing; meta-stability violations"
    observed_delta: "not isolated in validation suite"
  - id: E008
    cause: temporal_order
    effect: synergy
    intervention: desync stage_order
    predicted_change: "synergy and interaction_gain decrease"
    observed_delta: "synergy +0.014 (refuted); emergence_composite +0.0003"
