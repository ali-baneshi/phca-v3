[pytest]
markers =
    slow: Marks tests as slow (stress tests, multi-cycle runs). Use -m slow to run.
    timeout: Marks tests with a maximum runtime (seconds).
filterwarnings =
    ignore:.*StructureScore.*:FutureWarning:pgmpy.*
