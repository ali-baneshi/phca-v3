Plan and implement Observatory Phase 19 for PHCA v3.0 at /home/<username>/Pictures/Autonomous-AI-june2026/new-ai.

Context: Project has benchmarks, causal gates, assumption validation, and docs/reproducibility.md — but no single one-command reproduce manifest.

Phase 19 gate (STATUS.md): `make reproduce` one-command manifest reproducing key scientific artifacts on a clean machine.

Existing artifacts/commands:
- make test-all, make ci-local, make nightly NIGHTLY_CYCLES=10000
- scripts/benchmark.py, phca_causal_eval.py --gate, assumption_validation.py --ci
- logs/benchmark_report.json, phca_causal_eval.json, nightly_stress.json, assumption_validation.json
- docs/reproducibility.md, docs/benchmark_artifacts.md

Deliverables for the plan:
1. Makefile target `reproduce` (and optional `reproduce-quick` for CI-time budget)
2. Manifest file (e.g. reproduce_manifest.json) listing commands, expected output paths, hash or metric floors, runtime estimates
3. scripts/reproduce.py or shell driver: run suite sequentially, collect PASS/FAIL, write reproduce_report.json
4. Pin/document environment (Python 3.11, MUJOCO_GL=disabled) in manifest
5. Tests: smoke test of manifest structure; optional dry-run mode
6. Update docs/reproducibility.md as canonical entry point; STATUS.md Phase 19; DECISIONS.md

Constraints: honest floors from existing gates (do not tighten Φ-IQ to force pass); total runtime documented (~minutes for quick, ~hours for full).

Read Makefile, reproducibility.md, check_benchmark_gate.py, nightly targets. Plan the manifest scope before implementing.
