Plan and implement Observatory Phase 18 for PHCA v3.0 at /home/<username>/Pictures/Autonomous-AI-june2026/new-ai.

Context: Observatory has replay, scrub, session reports, and Phase 12 --compare. Phase 18 adds interactive querying.

Phase 18 gate (STATUS.md): `phca_query.py` CLI + dashboard filters for cognitive moments.

Existing hooks:
- cognitive_moment() flags: spike, learn_burst, decision_shift, violation, drive_change
- build_moment_series() / count_moments()
- moment_tab_badge() for SPIKE/VIOL/DECISION
- session_report spike_count, decision_shift_count, etc.

Deliverables for the plan:
1. scripts/phca_query.py CLI: query JSONL/session dir with filters (--spike, --violation, --explore, --drive N, --cycle-range, --near-bound MODULE)
2. Output modes: plain cycle list, JSON, optional export of matching frames
3. Dashboard filters: lightweight UI control (Overview or transport bar) to jump to next/previous moment matching filter; works in replay/review modes
4. Shared query engine module in phca/monitoring/ used by CLI + dashboard (no duplicated filter logic)
5. Tests: CLI on fixture sessions; dashboard filter navigation unit test
6. docs/observability.md usage section; STATUS.md; DECISIONS.md

Constraints: read-only queries must not mutate frames; performance OK on 3000+ cycle sessions (reuse decimation patterns from Phase 11).

Read cognitive_panels.py moment helpers, playback.py cursor, qt_dashboard transport. Plan first.
