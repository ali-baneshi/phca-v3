Plan and implement Observatory Phase 14 for PHCA v3.0 at /home/<username>/Pictures/Autonomous-AI-june2026/new-ai.

Context: Phases 7–12 complete. Phase 13 may or may not be done — if anomaly code exists, integrate with it; if not, proceed independently.

Phase 14 gate (STATUS.md): richer `action_rationale` in JSONL + an Explain panel (or dedicated sub-panel) showing causal chain from drive → goal → candidate scoring → chosen action.

Existing code:
- last_action_rationale populated in python/phca/core/cycle.py (_select_action, _select_continuous_action, RBTA safe mode, greedy_fallback, score_components)
- action_rationale already in ObservabilityFrame and JSONL (docs/observability.md)
- Action Selection tab in python/phca/monitoring/qt_dashboard.py already reads action_rationale
- classify_action_mechanism() in cognitive_panels.py / session_report mechanism histogram

Deliverables for the plan:
1. Schema extension for action_rationale (backward compatible — optional fields, no schema_version bump unless justified with migration)
2. Enrich cycle.py rationale: drive_id, mechanism, top score_components, decision reason (explore / greedy / prediction / D5-stay / RBTA-safe), relevant facts summary
3. Explain UI: replay-safe panel or Action tab subsection using ONLY JSONL fields (banner for live-only rollouts)
4. session_report + phca_replay --check parity for explain summaries
5. Tests: discrete GridWorld + continuous MPC paths; replay round-trip of new fields
6. Docs sync (observability.md field table, STATUS.md, DECISIONS.md)

Principles: no fake explanations — only serialize data the cycle actually computed; live-only candidate_rollouts stay bannered.

Read cycle.py action selection, observability.py frame serialization, qt_dashboard Action tab, session_report action_metrics. Produce plan first.
